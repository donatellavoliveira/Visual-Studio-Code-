// Garante que o JavaScript só será executado quando a página estiver carregada
document.addEventListener("DOMContentLoaded", () => {
    console.log("Página carregada com sucesso!"); // Exibe uma mensagem no console
});
